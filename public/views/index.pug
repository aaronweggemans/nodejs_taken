extends layout

block content
    .container
        .row
            .col-md-6.offset-3
                h1 Voeg een taak toe!
                form(action='/task' method='post')
                    input(type='text' class='form-control' name='title' placeholder='Task name')
                    input(type='submit' class='btn btn-primary' name='' value='Voeg taak toe')